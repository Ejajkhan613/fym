# Feature Boundaries

Frontend feature folders mirror the backend gateway domains and service
boundaries. Inventory is intentionally omitted from active UI structure for the
current phase.

Each feature owns its API calls, schemas, types, hooks, and route-specific UI as
implementation grows.
