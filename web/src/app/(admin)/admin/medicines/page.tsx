import { ModulePage } from "@/components/layout/module-page";

export default function AdminMedicinesPage() {
  return (
    <ModulePage
      eyebrow="Admin"
      title="Medicine catalog"
      description="Catalog management for brand-to-salt mapping, prescription requirement, restrictions, storage rules, substitution groups, synonyms, and CSV import."
    />
  );
}
